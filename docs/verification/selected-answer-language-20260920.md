# Selected response and speech language

STANDARD. Root sole writer; independent reviewer help_review found no remaining blocker. Owner authorized Preview response/speech fixes. No Production/master, phone routing, stored history or profile schema changes. Rollback 00a1ceb10dc0bfa2189b2d41973ded49e3fa350d.

Root cause: RC V3 chat only used signup metadata as a fallback and speech read supplied text literally, including English verification text. Existing RC language selection already persists ui_preferences.language and profiles.default_language. Shared resolver now reads the authenticated owner's latest profile: saved RC language, profile language, signup language, English fallback. Regional display locale only refines a matching language, so old en-AU display defaults cannot override Korean. RC V3 answer instructions and initial UI language use this resolver.

Playback resolves language again server-side and translates each complete chunk before TTS, even for old answers. Only already-used voice provider receives text; request has no secondary-provider fallback. Translation input is untrusted data, not instructions. Original messages are not rewritten. Output must be complete, nonempty and within 4,000 characters or playback fails visibly; no truncation. Translation and synthesis share a cancellable 32-second budget. Ownership, paid-service and request reservation checks precede provider calls. A language-change event cancels pending audio without clearing speaker ON.

Validation: 15 focused tests pass covering signup/selection precedence, stale display locale, invalid language, owner scoping, foreign-text translation, partial/oversized failure, translated text supplied to TTS, new language on next playback, access denial and existing persistent speaker cancellation/queue tests. Production build and TypeScript pass; scoped lint has zero errors (one existing image warning). Physical handset audibility is not established by cloud tests. Transcription language hints are outside this change.
