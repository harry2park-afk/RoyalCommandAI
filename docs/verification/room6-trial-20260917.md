# Room6 isolated trial — 2026-09-17

Status: implemented Preview trial; NOT functionally certified or approved for paid/global release.

STANDARD; writer root; independent reviewer voice_review. User authorized creation of a fresh trial alongside existing rooms, preserving Production/master and Katie telephone/recordings. Git rollback baseline: 91abb82eac80ce12e0e4f3c910a7edfab79e9408. No schema, billing, Retell or environment changes.

## Actual fault evidence

Preview deployment dpl_9bjBt1QGxR4vSc7jLv4p97N2n1ug returned two504s at04:36 UTC for /api/voice/realtime-session. Auth and SDP completed in about230–300ms. Provider stage timed out after15000ms without headers. This proves a failed server-to-provider exchange, not a broken physical microphone or the ultimate provider/network cause.

## Implemented trial

- Dedicated owner-only household and deterministic draft room, insert-only and duplicate-safe. A retry rechecks owner/identity; archived trials cannot be silently revived.
- Host created one authorized new draft room: 2ef6b11d-61e0-57b3-8aa9-0905e348da9b. Existing rows were not updated. Underlying DB is shared; draft may be listed by existing room lists. No claim of a separate database.
- New /room6/trial and APIs require Preview and authenticated owner of the draft. No user-provided room ID or provider credentials are accepted.
- Country, timezone, currency and independent login language use existing country presets. These are locale settings, NOT country-specific legal/payment/provider approval.
- Direct question response with server-loaded, provider-separated trial history. Append-only trial files in the existing private owner/room bucket prefix. No existing History/Memory is copied or modified.
- Request reservation precedes provider invocation; duplicate requests cannot automatically invoke twice. Trial daily budget100 requests, max1500 output tokens. Failed/pending requests require inspection rather than blind replay. Existing connectors may have their own internal fallbacks/timeouts; this trial does not certify these.
- Official ephemeral-credential WebRTC alternative: server mints short-lived transcription credential, browser exchanges SDP directly. Permanent key stays server-side; temporary value memory-only, no-store; hashed safety identifier. No provider configuration changes. Browser/device or provider network restrictions can still fail.
- Optional voice negotiation leaves legacy SDP route untouched. Text prefix retained, hidden-tab microphone stopped, transcript deltas shown, final text auto-submitted once, browser TTS then listening resumes. Browser language voice availability still requires real-device testing.
- Existing picture/button designer opens for this new draft; its chat button leads to the new trial. Secretary remains an optional existing launcher; account-wide secretary binding is not completed here.

## Evidence

36 targeted unit/route tests passed (28 baseline+creation+voice,5 trial chat/profile,3 voice token). Scoped eslint and Next production build/typecheck passed. Independent reviewer confirmed fixes for input draft loss, hidden capture and timeout mismatch. These tests use fake providers/devices/storage and are not live customer success evidence.

## Outstanding gates

Authenticated browser remained at login. No customer token was extracted or authentication bypassed. Actual browser storage write/reload, live AI reply, Korean microphone transcript and audible reply are NOT verified. Country-specific service availability and real accents/microphones must be tested. This is not a promise of no failures or all Level6 capabilities.

## References

https://developers.openai.com/api/docs/guides/voice-webrtc
https://developers.openai.com/api/docs/guides/realtime-transcription
https://supabase.com/docs/reference/javascript/insert
