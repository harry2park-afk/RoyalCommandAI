"use client";

/**
 * Council was retired from Royal Command.
 * Keep this no-op export temporarily so any stale import cannot break the Room
 * while the remaining legacy Council code paths are removed and verified.
 */
export default function CouncilModeToggle() {
  return null;
}
