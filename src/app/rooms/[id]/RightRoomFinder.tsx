"use client";

// Kept as a no-op compatibility component so the right app sidebar is unchanged.
// The active Room finder now lives in the top Room switcher row.
export default function RightRoomFinder() {
  return null;
}
