# Region Packs

Add a JSON Pack with a unique `region-*` ID, matching `regionCode`, and explicit `READY`, `NEEDS_REVIEW` or `BLOCKED` status. The build discovers it automatically. A runtime-enabled domain fails closed unless its Region Pack is READY.
